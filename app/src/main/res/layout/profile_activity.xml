<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/profileLayout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingEnd="@dimen/activity_horizontal_margin"
    android:paddingStart="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <FrameLayout
        android:layout_width="@dimen/profile_photo_progress_bar_size"
        android:layout_height="@dimen/profile_photo_progress_bar_size">

        <ImageView
            android:id="@+id/photoImageView"
            android:layout_width="@dimen/profile_photo_image_view_size"
            android:layout_height="@dimen/profile_photo_image_view_size"
            android:layout_gravity="center"
            android:contentDescription="@string/user_photo_content_description"
            android:transitionName="@string/transition_user_photo"
            tools:targetApi="lollipop"/>

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:indeterminate="true"
            android:visibility="gone"
            tools:visibility="visible"/>

    </FrameLayout>

    <TextView
        android:id="@+id/nameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:textSize="20sp"
        android:textStyle="bold"
        tools:text="John Doe"/>

    <TextView
        android:id="@+id/emailTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        tools:text="email@example.com"/>

    <Button
        android:id="@+id/signOutButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:text="@string/sign_out"/>

</LinearLayout>