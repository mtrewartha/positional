<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Material Theme Colors -->
    <color name="colorAccent">@color/green</color>
    <color name="colorPrimary">@color/gray1</color>
    <color name="colorPrimaryDark">@color/black</color>

    <!-- Generic UI Components -->

    <!-- Compass -->
    <color name="compassAttributeTitleColor">@color/gray5</color>
    <color name="compassAttributeValueColor">@color/gray5</color>
    <color name="compassBackground">@color/white</color>
    <color name="compassDegrees">@color/white</color>

    <!-- Navigation Bar -->
    <color name="navigationBarBackground">@color/black</color>
    <color name="navigationBarIconTint">@color/white</color>
    <color name="navigationBarItemBackground">@color/black</color>
    <color name="navigationBarTextColor">@color/white</color>

    <!-- Position Stats -->
    <color name="positionStatDivider">@color/gray1</color>
    <color name="positionStatTextColor">@color/white</color>
    <color name="positionStatIconTint">@color/white</color>

    <!-- View Pagers -->
    <color name="viewPagerTabDotSelected">@color/gray3</color>
    <color name="viewPagerTabDotUnselected">@color/gray1</color>
</resources>
